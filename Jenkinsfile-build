#!/usr/bin/env groovy

node ('evalg') {
    def workspace = pwd()
    def project = 'usit-int'
    def image_version

    stage ('Checkout') {
        checkout scm
        image_version = sh(
            returnStdout: true,
            script: 'git describe --dirty=+ --tags'
        ).trim() - ~/^v/
    }
    stage ('Build ballotbox image') {
        sh "docker build \
            -t '${project}/valg-ballotbox:${image_version}' \
            -f Dockerfile-ballotbox ."

    }
    stage ('Build evalg image') {
        sh "docker build \
            -t '${project}/valg-evalg:${image_version}' \
            -f Dockerfile-evalg ."
    }
    stage ('Push images') {
        sh "docker push '${project}/valg-evalg:${image_version}'"
        sh "docker push '${project}/valg-ballotbox:${image_version}'"
    }
}